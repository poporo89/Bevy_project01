// Before edit map data, :set lsp=4 without mode lines.

fn floor_set(x) {
    let floor = floor_new();
    floor.height = x;
    let data = [ 0, 0, 0];
    floor.data = data;
    floor
}

fn array_test() {
    let data = [
        [ 1, 1, 1,],
        [ 1, 1, 1,],
        [ 1, 1, 1,],
    ];
    data
}

